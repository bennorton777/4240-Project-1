/* Multi-line
comment */
let
        /*comment before token*/type/* comment after token*/ ArrayInt = array [100] of int; /*Declare ArrayInt as a new type */
        var X,/*comment between tokens*/Y : ArrayInt := 10; /* Declare vars X and Y as arrays with initialization */
        var i, sum :int := 0;
in
        for i := 1 to 100 do /* for loop for dot product */
                sum := sum + X[i] * Y [i];
        enddo;
        printi(sum); /* library call to printi to print the dot product */
end
/* comment at end */
